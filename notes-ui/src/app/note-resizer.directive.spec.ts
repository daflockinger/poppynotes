import { NoteResizerDirective } from './note-resizer.directive';

describe('NoteResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteResizerDirective();
    expect(directive).toBeTruthy();
  });
});
